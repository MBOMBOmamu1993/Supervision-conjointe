"use client";

import Dashboard from "@/components/dashboard/Dashboard";

/* Entrée du Tableau de bord Tshuapa — nouveau shell de navigation
   (chargement → bienvenue → accueil 6 onglets → vue module). */
export default function Page() {
  return <Dashboard />;
}
